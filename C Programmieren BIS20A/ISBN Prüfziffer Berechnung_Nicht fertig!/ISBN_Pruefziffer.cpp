#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {
    char eingabe[20];
    int berechnung[20];
        
    printf("Geben Sie bitte eine 9 stellige Nummer ein: %c", eingabe[20]);
    fgets (eingabe, 21, stdin);
    
    for (int i = 0; i < 15; i++) {
        berechnung[i] = eingabe[i] - '0';

    

   	getchar();             
   	getchar();     		
	return 0;

}
