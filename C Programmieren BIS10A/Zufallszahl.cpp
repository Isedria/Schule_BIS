#include <stdio.h>
#include <math.h>

float zufallszahl(int z);

main()
{
	int x, e;
	// e = Eingabe
	int zaehler = 0;
	
	x = zufallszahl(float z);
	
	while(zaehler < 10)
	{
	zaehler = zaehler +1;
	
	if x = e
		printf("Hurra!!");
	else{
		printf("Versuch es nochmal!");
		if x > e
			printf("Eingabe ist kleiner!");
		else
			printf("Eingabe ist größer!");
		}
	}
}
	

float zufallszahl(float z)
{
	z = rand();
	z = z/32768;
	z = (int) Z;
	return z;
}
